% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_network.R
\name{compute_network}
\alias{compute_network}
\title{Estimate network(s) from data}
\usage{
compute_network(
  data,
  vars,
  group_var = NULL,
  method = "EBICglasso",
  tuning = 0.25
)
}
\arguments{
\item{data}{A data frame containing the input data.}

\item{vars}{Character vector of column names to include in the network.}

\item{group_var}{(Optional) A column name used to split the data into groups (e.g., "group"). If NULL, estimates a single network.}

\item{method}{The network estimation method (e.g., "EBICglasso", "glasso", "pcor").}

\item{tuning}{Tuning parameter passed to `bootnet`. Default is 0.25.}
}
\value{
A list containing:
\describe{
  \item{networks}{A list of one or more `bootnetResult` objects.}
  \item{groups}{Character vector of group names (if grouping used), otherwise NULL.}
}
}
\description{
Estimates one or more networks using `bootnet::estimateNetwork()`.
}
\examples{
set.seed(42)
df <- MASS::mvrnorm(100, mu = rep(0, 4), Sigma = diag(4))
df <- as.data.frame(df)
colnames(df) <- c("A", "B", "C", "D")
compute_network(df, vars = c("A", "B", "C", "D"))

}
